import React, { Component } from 'react';

export default class Overview extends Component {
    render() {
        return (
            <h1>Overview page, must be authenticated to see this</h1>
        );
    }
}
